# Reservation_web_app это веб приложение, позволяющие создавать брони столиков (например в ресторане). Приложения написанно на
# FastAPI, в качестве базы данных использовалась: PostgreSQL, для работы с ней использовалась SQLAlchemy.
# В файле .env указаны переменные для подключения к бд. Параметры: DB_HOST, DB_PORT и DB_USER обычно одинаковы при стандартной установке бд.
# Параметры: DB_PASS и DB_NAME у каждой бд, свои. Их нужно ввести в файл env для корректной работы.